const Second=()=> {
    var val=7;

    val = val ==4 ? console.log('if statement'):console.log('else statement');
    return (
        <div>
            <h2>{ Math.round(8.4) }</h2>
        </div>
    )
}

export default Second